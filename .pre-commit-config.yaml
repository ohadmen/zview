repos:
  - repo: https://github.com/pocc/pre-commit-hooks
    rev: v1.3.5
    hooks:
      - id: clang-format
        args: [-i, --style=Google]
      - id: clang-tidy
        args: [--fix-errors, -warnings-as-errors=*]
      - id: cppcheck
        args: [--enable=all]
      - id: cpplint
        args: [--verbose=0, --filter=-legal/copyright]